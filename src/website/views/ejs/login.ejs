<!DOCTYPE html>
<html>

<head>
    <%- include('partials/head') %>
</head>

<body>
    <div class="container">
		<%- include('partials/header') %>
        <section class="small-form-content row align-items-center justify-content-center">
            <div class="col-xl-4 col-lg-5 col-md-6 col-sm-8 col-10" id="form">
                <h5 class="mb-4">Sign In</h5>
                <div class="row justify-content-left mb-3 m-0 p-0">
                    <% if(locals.wrongLoginData === true) { %>
                        <small class="error"><i class="fa fa-times-circle"></i> The fields you entered is wrong! Please try again.</small>
                    <% } else if (locals.fieldEmpty === true) { %>
                        <small class="error"><i class="fa fa-times-circle"></i> Fields cannot be empty.</small>
                    <% } %>
                </div>
                <form action="" method="POST">
                    <div class="form-group">
                        <label for="usernameOrEmail" class="input-label">Username / Email</label>
                        <input type="text" class="form-control" id="usernameOrEmailInput" name="usernameOrEmail" placeholder=""
                            required value="<%=usernameOrEmail%>">
                    </div>
                    <div class="form-group">
                        <label for="password" class="input-label justify-content-between">Password<a class="link" href="/forgot-password" style="float: right; text-align: right">Forgot your password?</a></label>
                        <input type="password" class="form-control" id="passwordInput" name="password" placeholder=""
                            required>
                    </div>
                    <div class="row justify-content-center m-0 p-0">
                        <small>Don't have an account yet? <span><a class="link" href="/register"> Sign up now!</a></span></small>
                    </div>
                    <button class="primary-button mt-4 p-2" type="submit">
                        <span>Sign in</span>
                    </button>
                    
                </form>
            </div>
        </section>
	</div>
    <%- include('partials/footer') %>
</body>

</html>